<div class="container">
  <% if current_user %>
  <h1>New Book</h1>

  <%= render 'form', book: @book %>

  <%= link_to 'Back', books_path %>
  <% else %>
  <p><%= link_to "Log In", new_user_session_path %>/<%= link_to "Sign Up", new_user_registration_path %> to create a book.</p>
  <% end %>
</div>
